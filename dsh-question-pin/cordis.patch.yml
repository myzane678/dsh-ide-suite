# dsh-question-pin bundle patch: registers the dual-face plugin row.
# Node half (exports ".") is a no-op loader entry; the dsh.client declaration
# in package.json makes the browser half (exports "./client") load in the
# web GUI.
- insert:
    - id: ui-dsh-question-pin
      name: 'dsh-question-pin'
